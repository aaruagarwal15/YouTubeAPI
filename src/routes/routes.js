const defaultHandler = require('../handlers/getApihandler');
const getApiHandler = require('../handlers/getApihandler');
const searchApiHandler = require('../handlers/searchApihandler');

const routes = (router) => {

}

module.exports = routes;
